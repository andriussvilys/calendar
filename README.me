This application uses javascript modules so a server is needed to run it. 
For VSCode users I suggest 'Live Server' extension.
Alternatively, you may use npm package http-server https://www.npmjs.com/package/http-server